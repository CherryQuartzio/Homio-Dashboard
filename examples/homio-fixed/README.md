# Homio Fixed snapshot

Exported from HA storage dashboard `homio-fixed` (config_hash `f12bfa33f415d5a3`, HA ~2026.8.3).

Working daily-driver config after thermostat (cool/heat), climate-based room temp, dimmable `homio_light` + `my-slider-v2` (module), theme/scroll resources, and Chromium background CSS fixes.

Portable templates live under `custom_components/homio_dashboard/dashboards/templates/`. See repo root `FORK_NOTES.md` for the full change log and resource types.
