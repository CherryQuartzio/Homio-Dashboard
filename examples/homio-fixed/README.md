# Homio Fixed snapshot

Exported from HA storage dashboard `homio-fixed` (config_hash `a6d1f86b99a9c0d0`).

This is the working daily-driver config. Portable template fixes also live under `custom_components/homio_dashboard/dashboards/templates/`.
